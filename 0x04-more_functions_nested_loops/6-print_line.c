#include "main.h"

/**
 * print_line - print a straight line using '_'
 * @n: The new number of '_' to be printed
 * Return: 0
 */
void print_line(int n)
{
	print_line(0);
	print_line(2);
	print_line(10);
	print_line(-4);
	return (0);
}
